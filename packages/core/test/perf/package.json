{
  "name": "most-perf",
  "version": "0.10.0",
  "description": "Perf tests for most.js",
  "author": "brian@hovercraftstudios.com",
  "license": "MIT",
  "scripts": {
    "chain": "babel-node --presets env ./chain.js",
    "concatMap": "babel-node --presets env ./concatMap.js",
    "filter-map-reduce": "babel-node --presets env ./filter-map-reduce",
    "merge": "babel-node --presets env ./merge.js",
    "merge-nested": "babel-node --presets env ./merge-nested.js",
    "sample": "babel-node --presets env ./sample.js",
    "scan": "babel-node --presets env ./scan.js",
    "slice": "babel-node --presets env ./slice.js",
    "skipRepeats": "babel-node --presets env ./skipRepeats.js",
    "switch": "babel-node --presets env ./switch.js",
    "zip": "babel-node --presets env ./zip.js",
    "start": "npm run filter-map-reduce && npm run chain && npm run concatMap && npm run merge && npm run merge-nested && npm run zip && npm run scan && npm run slice && npm run skipRepeats && npm run switch"
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-register": "^6.26.0",
    "baconjs": "^3.0.3",
    "benchmark": "^2.1.4",
    "highland": "^2.13.4",
    "kefir": "^3.8.6",
    "rxjs": "^6.5.2",
    "xstream": "^11.11.0"
  }
}
